@model EquipmentRentalCore.Models.AccountViewModels.ManageUserViewModel
@{
    ViewData["Title"] = "Manage your account";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewData["Title"]</h2>
<div class="row">
    <div class="col-md-4">
        <form asp-route-returnUrl="@ViewData["ReturnUrl"]" method="post">
            <h4>Manage your account</h4>
            <hr />
            <div asp-validation-summary="All" class="text-danger"></div>
            <input asp-for="Id" hidden />
            <div class="form-group">
                <label asp-for="Username"></label>
                @Model.Username
                <span asp-validation-for="Username" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FirstName"></label>
                <input asp-for="FirstName" class="form-control" />
                <span asp-validation-for="FirstName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Surname"></label>
                <input asp-for="Surname" class="form-control" />
                <span asp-validation-for="Surname" class="text-danger"></span>
            </div>
            <div>
                <button type="button" class="btn btn-info" data-toggle="modal" data-target="#ChangePasswordModal">Change your password</button>
                <button type="submit" class="btn btn-success">Submit</button>
                <hr />
                <a asp-area="" asp-action="Index" asp-controller="Home" class="btn btn-sm btn-danger" >Cancel</a>
            </div>
        </form>
    </div>
</div>
<div class="modal fade" id="ChangePasswordModal" role="dialog">
    <div class="modal-dialog">
        @await Html.PartialAsync("EditPassword")
    </div>
</div>
@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}

